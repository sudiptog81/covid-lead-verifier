# COVID Leads Verifier

## Quick Start

```bash
git clone https://github.com/sudiptog81/covid-lead-verifier.git
cd covid-lead-verifier
python3 -m venv venv
source venv/bin/activate
pip3 install -r requirements.txt
```
